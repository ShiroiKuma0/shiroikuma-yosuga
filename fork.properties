# Fork build counter for shiroikuma-yosuga.
# Bumped by _scripts/build-fork.sh after every successful build.
# Resets to 1 on each new upstream Memento version (see /upstream-new-version).
BUILD_NUMBER=10
